import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saksham.teenpatti',
  appName: 'Teen Patti Saksham',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
